try {
    $desktop = [Environment]::GetFolderPath('Desktop')
    $file = Join-Path $desktop 'hello.txt'

    "مرحبا" | Out-File -FilePath $file -Encoding UTF8 -Append
    Write-Host "تمت الكتابة بنجاح إلى: $file"
}
catch {
    Write-Error "حدث خطأ: $($_.Exception.Message)"
}
